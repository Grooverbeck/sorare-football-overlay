# Optional Sorare credentials for `wrangler dev`.
# Copy to `.dev.vars`; never commit the real values.
MOCK_MODE=true
SORARE_API_KEY=
SORARE_AUTH_TOKEN=
SORARE_JWT_AUD=

# Server-only key for real goal/assist bookmaker markets.
# Create one at https://the-odds-api.com and never add it to the extension.
THE_ODDS_API_KEY=
ODDS_API_FALLBACK_REGION=uk
SPORTS_GAME_ODDS_API_KEY=
ODDS_API_IO_KEY=
